---
'@gyxer-studio/generator': patch
---

Refactor editor UI into 4 tabs (Project, DB, Modules, HTTP), fix auth module auto-creating User entity, add prisma seed generation for test user, replace logo SVG.
