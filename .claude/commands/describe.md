Show the current state of the Gyxer Studio project.

## Instructions

1. Call `get_project` MCP tool to read the project schema
2. Call `list_entities` MCP tool for a formatted entity overview
3. Present a clear summary including:
   - Project name, description, database
   - All entities with their fields (name, type, constraints)
   - All relations between entities
   - Enabled modules and their options
4. Suggest next steps if the project looks incomplete
